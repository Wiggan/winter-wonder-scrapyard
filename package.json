{
  "name": "scrappy-server",
  "version": "0.0.1",
  "description": "Scrappy dedicated server",
  "dependencies": {
    "chance": "^1.0.18",
    "freedns-api": "^1.0.0",
    "mathjs": "^5.4.0",
    "self-reload-json": "^0.4.0",
    "socket.io": "^2.2.0",
    "vectors": "^0.1.0"
  },
  "devDependencies": {
    "concurrently": "^4.1.0"
  },
  "scripts": {
    "server": "node index.js",
    "client": "node start-client.js",
    "start": "concurrently \"npm run server\" \"npm run client\""
  }
}
